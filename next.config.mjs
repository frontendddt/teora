// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

// const nextConfig = {
//   images: { unoptimized: true },
//   output: 'export', 
// };

// export default nextConfig;

const nextConfig = {
  images: { unoptimized: true },
};

export default nextConfig;
